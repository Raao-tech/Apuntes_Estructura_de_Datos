/** 
*   @file   lista.h
*   @brief  Cabecera de la estructura de una lista enlazada simple
*   @author Rafael Araujo
*   @date   2026-04-15
*/
#ifndef LISTA_H
#define LISTA_H
/** 
 * @brief Estructura de una lista básica.
 * @note Una lista es una estructura de datos lineal (TAD) que permite acceder a 
 *       sus nodos con un bajo coste de operación de inserción/borrado.
 */
typedef struct _List List;

/*Métodos de una Lista*/

/*===============(Create/Destroy) Lista ===============*/
/**
*   @brief  Crea una lista vacía y devuelve un puntero a la nueva lista
*   @return Un **puntero** a la nueva lista vacía.
*/
Lista* list_create();
/**
*   @brief  Crea una lista vacía y devuelve un puntero a la nueva lista
*   @return Un **puntero** a la nueva lista vacía.
*/
Lista* list_create();






#endif
